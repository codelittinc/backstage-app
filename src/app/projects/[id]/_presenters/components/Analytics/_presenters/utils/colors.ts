export const getChartItemColor = (index: number) => {
  const colors = [
    "primary",
    "secondary",
    "info",
    "success",
    "warning",
    "error",
    "dark",
  ];

  return colors[index];
};
